diff --git a/src/App.jsx b/src/App.jsx
index efe4d95..ad0d6e1 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -1515,0 +1516,8 @@ if (customType === 'dosy') {
+      // La liste locale peut ├¬tre en retard sur un changement fait par la
+      // personne elle-m├¬me (POST /api/auth/change-password) : le serveur garde
+      // alors SA copie et le signale, sinon la publication ferait
+      // silencieusement revenir l'ancien mot de passe.
+      if (Array.isArray(res.staleIgnored) && res.staleIgnored.length) {
+        console.warn('[auth] publication ignor├⌐e pour', res.staleIgnored.join(', '),
+          'ΓÇö un mot de passe plus r├⌐cent est d├⌐j├á enregistr├⌐ sur le serveur (changement fait par la personne elle-m├¬me).');
+      }
@@ -4901,0 +4910 @@ const openDataset = (dset) => {
+              serverMode={serverLoginMode}
@@ -5004,0 +5014 @@ const openDataset = (dset) => {
+              serverMode={serverLoginMode}
