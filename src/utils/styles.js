/* =========================================================================
   src/utils/styles.js
   Shared tailwind class strings used across the app's form/calculator UI.
   (Extracted from App.jsx so sections split out of it stay consistent.)
   ========================================================================= */

export const CALC_INPUT_CLS =
  'w-full border border-slate-300 rounded-lg px-3 py-2 text-sm outline-none focus:border-blue-500 bg-white';

export const CALC_LABEL_CLS = 'block text-[10px] font-bold text-slate-400 uppercase mb-1';
