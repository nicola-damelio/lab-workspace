121: const SortHeader = ({ col, colIndex = 0, sort, onSort, alignRight, stickyLeft = null, stickyEdge = false }) => {
129: const stickyStyle = stickyLeft == null
132: left: stickyLeft,
134: boxShadow: stickyEdge ? '10px 0 10px -8px rgba(15, 23, 42, 0.4)' : undefined,
137: <th
138: className={`px-3 py-2.5 sticky top-0 z-10 bg-slate-50 border-b-2 border-slate-200 ${alignRight ? 'text-right' : 'text-left'}${divider}`}
139: style={stickyStyle}
207: const [stickyInfo, setStickyInfo] = useState({ active: false, lefts: [] });
456: colonnes (en-têtes + cellules) reçoivent `position: sticky; left: X` où X
465: setStickyInfo((prev) => (prev.active || prev.lefts.length ? { active: false, lefts: [] } : prev));
472: setStickyInfo((prev) => (prev.active || prev.lefts.length ? { active: false, lefts: [] } : prev));
477: défilé — ces en-têtes sont déjà « sticky » et renverraient leur position
488: setStickyInfo((prev) => (
526: target.querySelectorAll('td.sticky').forEach((td) => {
739: <table className="w-full text-sm border-collapse" style={{ minWidth }}>
740: <thead>
750: stickyLeft={stickyInfo.active && ci < stickyInfo.lefts.length ? stickyInfo.lefts[ci] : null}
751: stickyEdge={stickyInfo.active && ci === stickyInfo.lefts.length - 1}
756: <tbody>
778: const frozenLeft = stickyInfo.active && ci < stickyInfo.lefts.length
779: ? stickyInfo.lefts[ci]
790: frozenLeft != null ? 'sticky' : '',
793: <td
803: boxShadow: ci === stickyInfo.lefts.length - 1
