
  ! eslint(no-unused-vars): Identifier 
'cfgAxisTicks' is imported but never used.
  ! eslint(no-unused-vars): Identifier 
'cfgAxisDomain' is imported but never used.
  ! eslint(no-unused-vars): Identifier 
'cfgLogScale' is imported but never used.
  ! eslint(no-unused-vars): Identifier 
'mdLineDash' is imported but never used.



 524 |         id: `${entry.code3 || 
char}${index + 1}`,
 526 |         color: RESIDUE_COLORS[index 
% RESIDUE_COLORS.length],
 4042 |     const binX = Math.max(1, 
Math.ceil(visW / MAX_COLS));
 4049 |     for (let cx = 0; cx < nCols; 
cx++) {
 4050 |       const s0 = vis0 + cx * binX;
 4051 |       const s1 = Math.min(nSamples 
- 1, s0 + binX - 1);
 4067 |         out.push({ x: margin.left + 
cx * cw, y: margin.top + cy * ch, w: cw + 
0.5, h: ch + 0.5, letter: best });
 4042 |     const binX = Math.max(1, 
Math.ceil(visW / MAX_COLS));
 4049 |     for (let cx = 0; cx < nCols; 
cx++) {
 4050 |       const s0 = vis0 + cx * binX;
 4051 |       const s1 = Math.min(nSamples 
- 1, s0 + binX - 1);
 4067 |         out.push({ x: margin.left + 
cx * cw, y: margin.top + cy * ch, w: cw + 
0.5, h: ch + 0.5, letter: best });


